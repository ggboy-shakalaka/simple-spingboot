pipeline {
    agent any

    parameters {
        string(name:'TAG_NAME', description:'input tag')
    }

    environment {
        AAA_BBB_ID = 'dockerhub-id'
    }

    stages {
        stage('Test') {
            steps {
                echo 'Testing000..'
            }
        }
    }
}
